proxy_http_version 1.1;

location /api/ { proxy_pass http://backend.saikrishna.cloud:8080/; }

location /health {
  stub_status on;
  access_log off;
}

# backend.roboshop.internal
#{{ lookup('aws_ssm', 'frontend.con_url', region='us-east-1' ) }}

#{{ lookup('aws_ssm', 'frontend.con_url', region='us-east-1' ) }}